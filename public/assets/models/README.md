# 3D Models Directory

Place `.glb` or `.gltf` 3D models here (e.g. `room.glb`, `ghost_woman.glb`, `chair.glb`).
Use `RoomBuilder.loadGLBModel('/assets/models/your_model.glb', (gltf) => { ... })` to load them into the Three.js scene.
